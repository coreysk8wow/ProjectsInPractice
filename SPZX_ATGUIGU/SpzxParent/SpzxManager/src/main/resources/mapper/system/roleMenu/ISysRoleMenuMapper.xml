<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.spzx.manager.mapper.ISysRoleMenuMapper">

    <select id="selectMenuIdsByRoleId" resultType="long">
        select menu_id from sys_role_menu
        where role_id = #{roleId} and is_deleted = 0 and is_half = 0
    </select>

    <insert id="insertRoleMenuByRoleId">
        insert into sys_role_menu (
        role_id,
        menu_id,
        create_time , update_time , is_deleted , is_half
        ) values
        <foreach collection="menuIdList" item="menuInfo" separator=",">
            (#{roleId} , #{menuInfo.id} , now() , now() , 0 , #{menuInfo.isHalf})
        </foreach>
    </insert>

    <delete id="deleteByRoleIdAndMenuId">
        DELETE FROM sys_role_menu
        WHERE role_id = #{roleId}
        AND menu_id IN
        <foreach collection="menuIdList" item="menuInfo" open="(" separator="," close=")">
            #{menuInfo.id}
        </foreach>
    </delete>

    <delete id="deleteByMenuId">
        DELETE FROM sys_role_menu
        WHERE menu_id = #{menuId}
    </delete>

    <delete id="deleteByRoleId">
        DELETE FROM sys_role_menu
        WHERE role_id = #{roleId}
    </delete>

    <update id="updateIsHalfByMenuId">
        UPDATE sys_role_menu
        SET is_half = #{isHalf}, update_time = now()
        WHERE menu_id = #{menuId}
        AND is_deleted = 0
    </update>

</mapper>