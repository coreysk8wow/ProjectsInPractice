<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.spzx.manager.mapper.ISysUserRoleMapper">

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,role_id,user_id,create_time,update_time,is_deleted
    </sql>

    <select id="getRoleIdsByUserId" resultType="java.lang.Long">
        select
        role_id
        from sys_user_role
        <where>
            <if test="user_id != null">
                and user_id = #{userId}
            </if>
            and is_deleted = 0
        </where>
        order by id desc
    </select>
    
    <delete id="deleteByUserId">
    delete from sys_user_role
    where user_id = #{userId}
    </delete>
    
    <delete id="deleteByUserIdAndRoleId">
        delete from sys_user_role
        where user_id = #{userId} and role_id = #{roleId}
    </delete>

    <insert id="insertUserRoleByUserId">
        insert into sys_user_role
        (role_id, user_id, create_time, update_time, is_deleted)
        values
        (#{roleId}, #{userId}, now(), now(), 0)
    </insert>
</mapper>
